spring.application.name=community

# 1. 데이터베이스 연결 설정 (RDS)
spring.datasource.url=jdbc:mysql://team5-db.cliewcci82ex.ap-northeast-2.r         ds.amazonaws.com:3306/team5_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=admin
spring.datasource.password=12345678
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 2. 자동 테이블 업데이트 설정 (validate에서 update로 변경하여 편리함 확보)
spring.jpa.hibernate.ddl-auto=update

# 3. 파일 업로드 크기 제한
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# 4. AWS S3 설정 (직접 사용할 버킷 이름 명시)
app.s3.bucket=team5-s3-719279823221-ap-northeast-2-an

# 5. SQL 디버깅 설정
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 6. 외부 접속 허용 (EC2에서 띄울 경우 필요)
server.address=0.0.0.0
server.port=8080